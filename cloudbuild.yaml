steps:
- name: 'docker'
  entrypoint: '/bin/sh'
  args: 
  - '-e'
  - '-c'
  - |-
    apk add bash
    docker login --username=coquinone --password=$_PASS
    ./build_push_update_images.sh v1 --prefix=coquinone
